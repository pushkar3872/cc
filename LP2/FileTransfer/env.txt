create vpc (add name only no other conf)

create 1st ec2 instance
- change network settings
- change vpc to the created vpc
- change subnet from private to public
- enable auto assign ip

create 2nd ec2 instance
- change network settings
- change vpc to created vpc
- keep subnet private 
- keep auto assign ip disabled
- edit inbound security rules 
(
    in ssh change source type to custom
    and select 1st instance's security group
)

// in local
chmod 400 k1.pem
ssh -i k1.pem ubuntu@3.235.86.238

// in A
nano text.txt
exit

// in local
scp -i k1.pem k2.pem ubuntu@3.235.86.238:~  
ssh -i k1.pem ubuntu@3.235.86.238

// in A
ls
chmod 400 k2.pem
scp -i k2.pem text.txt ubuntu@10.0.154.205:~
ssh -i k2.pem ubuntu@10.0.154.205

// in B
ls
cat text.txt